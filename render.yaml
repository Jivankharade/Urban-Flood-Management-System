services:
  - type: web
    name: urban-flood-nowcasting-api
    runtime: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    envVars:
      - key: ALLOWED_ORIGINS
        sync: false
      - key: AI_ENGINE_URL
        value: http://localhost:8000